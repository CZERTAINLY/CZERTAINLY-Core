{
  "type": "object",
  "required": ["sequence"],
  "properties": {
    "sequence": {
      "type": "array",
      "maxItems": 2,
      "prefixItems": [
        {
          "type": "object",
          "required": ["boolean"],
          "properties": { "boolean": { "const": true } }
        },
        {
          "type": "object",
          "required": ["integer"],
          "properties": { "integer": { "type": "integer", "minimum": 0 } }
        }
      ]
    }
  }
}
